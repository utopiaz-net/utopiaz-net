---
const { subtitle, title, subtext } = Astro.props;
---

<p class="text-brand font-bold text-center">{subtitle}</p>
<h4 class="pb-6 font-semibold text-3xl text-center">{title}</h4>
<p class="pb-6 text-gray-600 dark:text-gray-400 text-center text-lg">{subtext}</p>
